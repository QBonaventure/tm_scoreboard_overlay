<%= case @overlay do
  nil -> content_tag :span, "There's no live overlay !"
  _ -> live_component @socket, TMSOWeb.OverlayComponent, overlay: @overlay
end %>


<section id="overlays-list">
  <%= for overlay <- @overlays do
    render("overlay-item.html", overlay: overlay)
  end %>
</section>
